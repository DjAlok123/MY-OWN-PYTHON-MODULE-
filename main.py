# In this project I have made a own function to print, the name of the function is display()
# I have also made a math module too!.
# math module eg.
# 1. the add() function
# 2. the sub() function
# 3. the multi() function
# 4. the divide() function

# to use the add function type add() and inside the brackets type your number eg.add(2,3) this will display 5 as output
# same to all of the other function 
# use the all math function inside the display function

# this are the examples of the function.

from math_cool import *

display(add(5,5))
display(sub(5,3))
display(multi(5,5))
display(int(divide(48,2)))